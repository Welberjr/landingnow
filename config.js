/* ============================================================
   Configuração de medição da landingnow V3.
   Os IDs ficam VAZIOS no código: preencha aqui na hora do deploy
   (equivalente estático da variável de ambiente; nada de segredo
   commitado). Com os campos vazios, a página funciona normalmente
   e nenhum script de terceiro é carregado.
   ============================================================ */
window.LN_CONFIG = {
  GA_MEASUREMENT_ID: '',   // ex.: G-XXXXXXXXXX
  CLARITY_PROJECT_ID: ''   // ex.: abcdefghij
};
